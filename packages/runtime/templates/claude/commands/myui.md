---
description: Use the myui skill to generate, polish, or refine UI components.
argument-hint: [target-file-or-region] [prompt]
---

Invoke the myui skill via the Skill tool.

Pass the full user request as context: $ARGUMENTS.

Follow the skill workflow exactly.
